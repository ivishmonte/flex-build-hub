import { ArrowUpRight, BarChart3, Bot, Check, Code2, Mail, Menu, MessageCircle, Sparkles, X } from "lucide-react";
import { useState, type ReactNode } from "react";
import { Button } from "@/components/ui/button";
import logoAsset from "@/assets/logo.png.asset.json";

const nav = [["Проекты", "#projects"], ["Услуги", "#services"], ["Процесс", "#process"], ["Обо мне", "#about"]] as const;

export function Header() {
  const [open, setOpen] = useState(false);
  return <header className="fixed inset-x-0 top-0 z-50 border-b border-border bg-background/75 backdrop-blur-2xl"><div className="mx-auto flex h-18 max-w-7xl items-center justify-between px-5 lg:px-8"><a href="#top" className="flex min-h-11 items-center gap-3 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><img src={logoAsset.url} alt="Логотип Алекс Нейро" className="size-10 rounded-xl object-cover" /><span><strong className="block text-sm text-foreground">Алекс Нейро</strong><span className="block text-xs text-muted-foreground">Специалист по нейросетям</span></span></a><nav className="hidden items-center gap-7 lg:flex" aria-label="Основная навигация">{nav.map(([label, href]) => <a key={href} className="text-sm text-muted-foreground transition-colors hover:text-foreground" href={href}>{label}</a>)}<Button asChild variant="hero"><a href="#contact">Связаться</a></Button></nav><Button aria-label={open ? "Закрыть меню" : "Открыть меню"} aria-expanded={open} variant="ghost" size="icon" className="size-11 lg:hidden" onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</Button></div>{open && <nav className="border-t border-border bg-background px-5 py-4 lg:hidden" aria-label="Мобильная навигация">{nav.map(([label, href]) => <a onClick={() => setOpen(false)} key={href} className="flex min-h-12 items-center border-b border-border text-base text-foreground" href={href}>{label}</a>)}<Button asChild variant="hero" size="hero" className="mt-4 w-full"><a onClick={() => setOpen(false)} href="#contact">Связаться</a></Button></nav>}</header>;
}

export function SectionHeading({ eyebrow, title, children }: { eyebrow?: string; title: string; children: ReactNode }) {
  return <div className="mb-10 max-w-2xl md:mb-14">{eyebrow && <p className="mb-3 text-sm font-semibold uppercase text-primary">{eyebrow}</p>}<h2 className="text-3xl font-bold text-foreground md:text-5xl">{title}</h2><p className="mt-5 text-base leading-7 text-muted-foreground md:text-lg">{children}</p></div>;
}

export function ProductDashboard() {
  return <div className="dashboard-float relative mx-auto w-full max-w-[650px] rounded-2xl border border-border-strong bg-surface-glass p-2 shadow-panel backdrop-blur-xl"><div className="overflow-hidden rounded-xl border border-border bg-surface"><div className="flex h-9 items-center gap-2 border-b border-border px-3"><i className="size-2 rounded-full bg-accent"/><i className="size-2 rounded-full bg-primary"/><i className="size-2 rounded-full bg-brand-pink"/><span className="ml-2 text-[10px] text-muted-foreground">neuro.space / overview</span></div><div className="grid grid-cols-[62px_1fr] md:grid-cols-[124px_1fr]"><aside className="border-r border-border p-2 md:p-3"><div className="mb-5 flex items-center gap-2 text-xs font-semibold"><span className="grid size-6 place-items-center rounded-md bg-primary"><Sparkles className="size-3 text-primary-foreground"/></span><span className="hidden md:block">NEURO</span></div>{[BarChart3, Bot, MessageCircle, Code2].map((Icon,i)=><div key={i} className={`mb-2 flex h-8 items-center gap-2 rounded-md px-2 ${i===0?"bg-secondary text-foreground":"text-muted-foreground"}`}><Icon className="size-3.5"/><span className="hidden text-[10px] md:block">{["Analytics","AI Assistant","Messages","Settings"][i]}</span></div>)}</aside><div className="min-w-0 p-3 md:p-5"><div className="mb-4 flex items-start justify-between"><div><p className="text-[9px] text-muted-foreground">Good morning, Alex</p><p className="text-sm font-semibold md:text-lg">Product overview</p></div><span className="rounded-md bg-success/15 px-2 py-1 text-[8px] text-success">Live</span></div><div className="grid grid-cols-3 gap-2">{[["12.4K","Users"],["+28%","Growth"],["$84K","Revenue"]].map(([v,l])=><div key={l} className="rounded-lg border border-border bg-card p-2 md:p-3"><p className="text-sm font-bold md:text-xl">{v}</p><p className="text-[8px] text-muted-foreground md:text-[10px]">{l}</p></div>)}</div><div className="mt-3 grid grid-cols-[1.5fr_1fr] gap-2"><div className="rounded-lg border border-border bg-card p-3"><div className="mb-5 flex justify-between text-[8px]"><span>Activity</span><span className="text-muted-foreground">This week</span></div><div className="flex h-20 items-end gap-1">{[35,52,42,72,58,88,66,94,74,100,82,96].map((h,i)=><i key={i} className="chart-bar flex-1 rounded-t-sm" style={{height:`${h}%`,opacity:.45+i*.045}} />)}</div></div><div className="rounded-lg border border-border bg-card p-3"><div className="mb-3 flex items-center gap-1 text-[8px]"><Bot className="size-3 text-primary"/>AI insight</div><p className="text-[8px] leading-4 text-muted-foreground">Retention вырос на 18%. Запустить новый onboarding?</p><div className="mt-3 flex items-center gap-1 text-[8px] text-success"><Check className="size-3"/>Ready</div></div></div></div></div></div><div className="absolute -bottom-6 -right-2 w-44 rounded-xl border border-border-strong bg-code p-3 shadow-soft md:-right-8 md:w-56"><div className="mb-2 flex items-center gap-2 text-[9px] text-muted-foreground"><Code2 className="size-3"/>agent.ts</div><pre className="overflow-hidden text-[7px] leading-3 text-code-foreground md:text-[9px] md:leading-4"><code><span className="text-brand-pink">const</span> insight = <span className="text-primary">await</span>{"\n"}  ai.analyze(data);{"\n"}<span className="text-success">// +18% retention</span></code></pre></div></div>;
}

export function ProjectPreview({ type }: { type: string }) {
  if (type === "landing") return <div className="preview-shell bg-preview-warm"><div className="mx-auto mt-5 w-[82%] rounded-lg border border-border bg-surface p-3 shadow-panel"><div className="mb-4 flex items-center justify-between"><span className="text-[8px] font-bold">LAUNCH<span className="text-accent">PRO</span></span><span className="rounded bg-accent px-2 py-1 text-[6px] text-primary-foreground">START NOW</span></div><p className="max-w-[70%] text-lg font-bold leading-tight">Build products people want.</p><p className="mt-2 max-w-[65%] text-[7px] text-muted-foreground">From idea to launch in record time.</p><div className="mt-4 h-14 rounded-md bg-brand-gradient opacity-80" /></div></div>;
  return <div className={type === "learning" ? "preview-shell bg-preview-cool" : "preview-shell bg-preview-violet"}><div className="grid h-full grid-cols-[46px_1fr] gap-2 p-4"><div className="rounded-lg border border-border bg-surface p-2"><div className="mb-3 size-5 rounded bg-primary"/>{[1,2,3,4].map(i=><div key={i} className="mb-2 h-1.5 rounded bg-muted"/>)}</div><div className="min-w-0"><p className="text-[9px] font-semibold">{type === "learning" ? "Learning progress" : "Analytics overview"}</p><div className="mt-3 grid grid-cols-3 gap-2">{[72,48,91].map(n=><div key={n} className="rounded-md border border-border bg-card p-2"><b className="text-xs">{n}%</b><div className="mt-2 h-1 rounded bg-primary/40"/></div>)}</div><div className="mt-2 flex h-16 items-end gap-1 rounded-md border border-border bg-card p-2">{[30,55,43,72,60,90,74,98].map((h,i)=><i key={i} className="chart-bar flex-1 rounded-t-sm" style={{height:`${h}%`}}/>)}</div></div></div></div>;
}

export function BuyCourseButton({ loading = false, disabled = false }: { loading?: boolean; disabled?: boolean }) {
 return <Button variant="hero" size="hero" disabled={disabled || loading} aria-busy={loading} className="min-w-44 max-w-full">{loading ? <><span className="spinner"/>Обработка...</> : "Купить курс"}</Button>;
}

export { ArrowUpRight, Mail, MessageCircle };